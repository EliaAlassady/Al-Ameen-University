export class Department {
    constructor(title, Icon, text) {
        this.title = title
        this.Icon = Icon
        this.text = text
    }
}

export class Lecture {
    constructor(stage, Icon) {
        this.stage = stage
        this.Icon = Icon
    }
}